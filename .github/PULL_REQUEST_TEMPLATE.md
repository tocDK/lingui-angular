## What & why

<!-- One paragraph: what does this PR do and why? -->

## Checklist

- [ ] `npm test` passes (44 tests, zero failures)
- [ ] `npm run lint` passes (zero errors)
- [ ] `npm run build:lib` succeeds — `dist/` is intact
- [ ] `npm run extract:check` passes (catalogs up to date)
- [ ] README / CHANGELOG updated if the public API changed
